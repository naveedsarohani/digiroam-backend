// user confidentials fields
const userConfidentialFields = '-password -__v';

// project populate options
const payment = [
    {
        path: 'userId',
        select: userConfidentialFields
    },
];

export default { payment };